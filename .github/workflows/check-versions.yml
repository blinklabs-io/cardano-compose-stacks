# Generated automatically by org-governance-bot. Do not edit manually.
name: "check-versions"

on:
  schedule:
    - cron: 0 0 * * 0
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  orchestrate:
    uses: blinklabs-io/actions/.github/workflows/reuseable-check-versions.yml@main
